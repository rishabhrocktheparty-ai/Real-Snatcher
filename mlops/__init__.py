# mlops package initializer

"""
Mark this directory as a Python package so the worker can be run with
`python -m mlops.ml_worker` and imports like `api` resolve from the
project root. This file intentionally left minimal.
"""
